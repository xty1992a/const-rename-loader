// app 初始化信息
export const SET_APP_CONFIG = 'SET_APP_CONFIG'
export const SET_STRATE_CONFIG = 'SET_STRATE_CONFIG'

const list = [
  'asdf',
  'asdf'
]


export const list2 = list
export const list3 = list.map((name, index) => ({name, id: index}))
export function tes() {
}
